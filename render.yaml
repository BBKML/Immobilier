services:
  - type: web
    name: gestion-vehicules
    env: python
    plan: free
    buildCommand: chmod +x ./build.sh && chmod +x ./start.sh && ./build.sh
    startCommand: ./start.sh
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16
      - key: DJANGO_DEBUG
        value: False
      - key: DJANGO_ALLOWED_HOSTS
        value: immobilier-khoa.onrender.com
      - key: DJANGO_SECRET_KEY
        generateValue: true
    healthCheckPath: /admin/
    autoDeploy: true
    # Ajouter un volume pour persister la base de données SQLite
    disk:
      name: database
      mountPath: /opt/render/project/src
      sizeGB: 1
